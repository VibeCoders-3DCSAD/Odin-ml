# Agent and assistant tooling
CLAUDE.md
.codex
.claude/

# Python virtual environments
.venv/
venv/

# Python cache and build output
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/

# Jupyter
.ipynb_checkpoints/

# Secrets and local env files
.env
.env.*
!.env.example
!.env*.example

# Generated datasets and pipeline outputs (regenerate with training scripts).
# NOTE: the top-level `models/` directory (new-scope FINAL artifacts +
# metadata.json) is intentionally tracked and must NOT be ignored.
training/datasets/
training/synth/
training/models/
training/figures/

# Synthetic Generation v1 / v2 outputs written at the repo root (regenerate
# with training/scripts/synthesizer.py or training/scripts/synthesizer_v2.py).
synth/
synth_v2/

# Platform and editor noise
.DS_Store
Thumbs.db
